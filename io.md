# IO

## path tiny

```perl
use v5.12;
use Path::Tiny;
# write 
my $filename = 'io.txt';
my $path = path($filename);
$path->spew("My first report generated by perl\n");

# read
my $filename = 'io.txt';
my $path = path($filename);
my $content = $path->slurp;
say $content;
```

